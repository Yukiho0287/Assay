export default function ChannelsPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold">渠道</h1>
      <p className="mt-1 text-sm text-muted-foreground">
        保存渠道基本信息：接入地址、支持协议、凭证（写后不可回读，仅展示脱敏前缀）。待实现。
      </p>
    </div>
  )
}
